The code performs merge sort on a array of integers using divide and conquere method.
The complexilty of the algorithm is O(N*log(N)),where is N is the number of elements in the data set.
The space complexity is O(n).
